/**
 * List of action patterns expected in managing the UI.
 */
export enum Actions {
  /**
   * Display the something is happening on the background.
   */
  FETCH_CURRENCY_CONVERSION_RATES = 'CURRENCY_CONVERSION/FETCH_CURRENCY_CONVERSION_RATES',

  /**
   * Display the something is happening on the background.
   */
  CACHE_CURRENCY_CONVERSION_RATES = 'CURRENCY_CONVERSION/CACHE_CURRENCY_CONVERSION_RATES',
}
